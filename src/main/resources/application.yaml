spring:
  kafka:
    bootstrap-servers: localhost:9092 # Your local Docker.
    consumer:
      group-id: "poc_group_id"

my:
  config:
    limit: 0
    topics:
      hot: "hot_topic"
      cold: "cold_topic"
